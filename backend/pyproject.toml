[tool.poetry]
name = "game-backend"
version = "0.1.0"
description = "Backend for the interactive game."
authors = ["Jules"]

[tool.poetry.dependencies]
python = "^3.9"
fastapi = "*"
uvicorn = {extras = ["standard"], version = "*"}
python-dotenv = "*"
pyautogen = "*"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
